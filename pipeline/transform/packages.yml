packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
  # dbt-expectations ports Great Expectations-style assertions into dbt tests —
  # the in-DAG complement to the standalone GX suite under ../quality.
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
